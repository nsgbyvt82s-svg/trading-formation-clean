'use client';

import React, { useState, useEffect } from 'react';
import Head from 'next/head';
import { motion, AnimatePresence } from 'framer-motion';
import { FiCheckCircle, FiTrendingUp, FiUser, FiClock, FiAward, FiDollarSign, FiBarChart2, FiShield } from 'react-icons/fi';
import LoadingScreen from './components/LoadingScreen';
import PromoModal from './components/PromoModal';
import { Header } from '@/components/layout/Header';
import { useAuth } from '@/hooks/useAuth';

// Interfaces TypeScript pour le typage
interface Course {
  title: string;
  description: string;
  icon: string;
  color: string;
}

interface Advantage {
  icon: React.ReactNode;
  title: string;
  description: string;
}

interface Testimonial {
  name: string;
  role: string;
  avatar: string;
  content: string;
  rating: string;
  profit: string;
}

const Home = () => {
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const [showPromoModal, setShowPromoModal] = useState(false);
  const [isHovered, setIsHovered] = useState(false);
  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 });
  const [isVisible, setIsVisible] = useState(false);
  const [particles, setParticles] = useState<Array<{width: string, height: string, left: string, top: string}>>([]);
  
  const { user, isAuthenticated } = useAuth();

  useEffect(() => {
    setIsVisible(true);
    // Générer les particules uniquement côté client
    setParticles(
      Array(10).fill(0).map(() => ({
        width: `${Math.random() * 6 + 2}px`,
        height: `${Math.random() * 6 + 2}px`,
        left: `${Math.random() * 100}%`,
        top: `${Math.random() * 100}%`,
      }))
    );
  }, []);

  // Animation des cartes de formation
  const container = {
    hidden: { opacity: 0 },
    show: {
      opacity: 1,
      transition: {
        staggerChildren: 0.2
      }
    }
  };

  const item = {
    hidden: { opacity: 0, y: 20 },
    show: { 
      opacity: 1, 
      y: 0,
      transition: {
        duration: 0.5
      }
    }
  };

  return (
    <div className="min-h-screen flex flex-col">
      <header className={`fixed w-full z-50 transition-all duration-300 ${scrolled ? 'bg-gray-900/90 backdrop-blur-md py-4' : 'bg-transparent py-6'}`}>
        <Header />
      </header>
      
      <div className="app-container flex-grow">
        <div className="pt-24">
          {/* Hero Section */}
          <section className="relative z-10 container mx-auto px-4 h-full flex flex-col justify-center py-32">
            <motion.div 
              className="hero-content max-w-5xl mx-auto text-center"
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.4 }}
            >
              <div className="inline-block mb-6 px-4 py-2 bg-blue-900/30 border border-blue-700/50 rounded-full text-blue-300 text-sm font-medium">
                🚀 Formation Trading Professionnel
              </div>
              
              <motion.div 
                className="text-5xl md:text-6xl lg:text-7xl font-bold mb-6"
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: 0.6 }}
              >
                {isAuthenticated ? (
                  <div>
                    <div className="bg-clip-text text-transparent bg-gradient-to-r from-white via-blue-200 to-blue-400">
                      Bonjour, <span className="text-blue-400">{user?.name || 'cher client'}</span> 👋
                    </div>
                    <p className="text-2xl md:text-3xl mt-4 text-blue-300">Bienvenue sur votre espace de trading</p>
                  </div>
                ) : (
                  <div 
                    className="bg-clip-text text-transparent bg-gradient-to-r from-white via-blue-200 to-blue-400"
                    dangerouslySetInnerHTML={{ __html: 'Devenez un <span class="text-blue-400">Trader Élite</span> avec TradeElite Pro' }}
                  />
                )}
              </motion.div>
              
              <motion.p 
                className="text-xl text-gray-300 mb-10 max-w-3xl mx-auto leading-relaxed"
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: 0.8 }}
              >
                Rejoignez la communauté de traders qui ont transformé leur vie grâce à nos formations complètes. 
                Accédez à des stratégies éprouvées, un accompagnement personnalisé et des outils exclusifs.
              </motion.p>
              
              <motion.div 
                className="flex flex-col sm:flex-row gap-4 justify-center mb-16"
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: 1 }}
              >
                <button 
                  onClick={() => setShowPromoModal(true)}
                  className="px-8 py-4 bg-gradient-to-r from-blue-700 to-blue-800 text-white rounded-xl font-semibold text-lg hover:opacity-90 transition-all transform hover:-translate-y-1 shadow-lg hover:shadow-blue-500/30 flex items-center justify-center gap-2"
                >
                  <FiTrendingUp className="text-xl" />
                  Commencer ma réussite
                </button>
                <button 
                  className="px-8 py-4 bg-blue-900/30 text-white border border-blue-700/50 rounded-xl font-semibold text-lg hover:bg-blue-900/50 transition-all transform hover:-translate-y-1 backdrop-blur-sm flex items-center justify-center gap-2"
                >
                  <FiUser className="text-xl" />
                  Voir les témoignages
                </button>
              </motion.div>
          
          {/* Statistiques */}
          <motion.div 
            className="grid grid-cols-2 md:grid-cols-4 gap-6 max-w-4xl mx-auto mt-12"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5, delay: 1.2 }}
          >
            <div className="bg-white/5 backdrop-blur-sm p-6 rounded-xl border border-white/10 text-center">
              <div className="text-3xl font-bold text-blue-400 mb-2">10,000+</div>
              <div className="text-gray-400">Étudiants formés</div>
            </div>
            <div className="bg-white/5 backdrop-blur-sm p-6 rounded-xl border border-white/10 text-center">
              <div className="text-3xl font-bold text-blue-400 mb-2">98%</div>
              <div className="text-gray-400">Satisfaction</div>

        {/* Section Avantages */}
        <section className="py-20 bg-gradient-to-b from-gray-900 to-gray-950 relative overflow-hidden">
          <div className="absolute top-0 left-0 w-full h-full opacity-5">
            <div className="absolute inset-0 bg-[url('/images/grid.svg')]"></div>
          </div>
          
          <div className="container mx-auto px-4 relative z-10">
            <motion.div 
              className="text-center mb-16"
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.6 }}
            >
              <h2 className="text-4xl md:text-5xl font-bold mb-6 text-white">Pourquoi choisir <span className="text-blue-400">TradeElite Pro</span> ?</h2>
              <p className="text-xl text-gray-400 max-w-3xl mx-auto">Découvrez ce qui fait de nous le partenaire idéal pour votre réussite en trading</p>
            </motion.div>
            
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-20">
              {[
                {
                  icon: <FiTrendingUp className="text-4xl" />,
                  title: "Résultats Concrets",
                  description: "Stratégies éprouvées avec des résultats vérifiables"
                },
                {
                  icon: <FiUser className="text-4xl" />,
                  title: "Mentorat",
                  description: "Accompagnement personnalisé par des experts"
                },
                {
                  icon: <FiClock className="text-4xl" />,
                  title: "Accès Illimité",
                  description: "Accès à vie à toutes les mises à jour"
                },
                {
                  icon: <FiAward className="text-4xl" />,
                  title: "Certification",
                  description: "Certification reconnue à la fin de la formation"
                }
              ].map((item: Advantage, index: number) => (
                <motion.div 
                  key={index}
                  className="bg-gray-900/50 backdrop-blur-sm p-8 rounded-2xl border border-gray-800 hover:border-blue-500/30 transition-all duration-300"
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.4, delay: index * 0.1 }}
                >
                  <div className="w-16 h-16 rounded-xl bg-blue-900/30 flex items-center justify-center mb-6 mx-auto">
                    {item.icon}
                  </div>
                  <h3 className="text-xl font-bold text-white mb-3">{item.title}</h3>
                  <p className="text-gray-400">{item.description}</p>
                </motion.div>
              ))}
            </div>
          </div>
        </section>
        
        {/* Section Formations */}
        <section className="py-20 bg-gradient-to-b from-gray-950 to-gray-900 relative overflow-hidden">
          <div className="absolute top-0 left-0 w-full h-full opacity-5">
            <div className="absolute inset-0 bg-[url('/images/grid.svg')]"></div>
          </div>
          
          <div className="container mx-auto px-4 relative z-10">
            <motion.div 
              className="text-center mb-16"
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.6 }}
            >
              <h2 className="text-4xl md:text-5xl font-bold mb-6 text-white">Nos Formations <span className="text-blue-400">Élite</span></h2>
              <p className="text-xl text-gray-400 max-w-2xl mx-auto">Découvrez nos programmes de formation conçus pour vous amener du niveau débutant à expert.</p>
            </motion.div>
            
            <motion.div 
              className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8"
              variants={container}
              initial="hidden"
              whileInView="show"
              viewport={{ once: true }}
            >
              {[
                {
                  title: "Trading Débutant",
                  description: "Maîtrisez les bases du trading et développez une stratégie gagnante.",
                  icon: "📊",
                  color: "linear-gradient(135deg, #1e40af, #1e3a8a)"
                },
                {
                  title: "Analyse Technique",
                  description: "Apprenez à analyser les graphiques et à identifier les tendances.",
                  icon: "📈",
                  color: "linear-gradient(135deg, #3b82f6, #2563eb)"
                },
                {
                  title: "Gestion du Risque",
                  description: "Protégez votre capital avec des techniques de gestion avancées.",
                  icon: "🛡️",
                  color: "linear-gradient(135deg, #10b981, #059669)"
                },
                {
                  title: "Trading Automatisé",
                  description: "Créez et testez vos propres algorithmes de trading.",
                  icon: "🤖",
                  color: "linear-gradient(135deg, #dc2626, #b91c1c)"
                },
                {
                  title: "Marchés Financiers",
                  description: "Comprenez les différents marchés et comment en tirer profit.",
                  icon: "🌐",
                  color: "linear-gradient(135deg, #d97706, #b45309)"
                },
                {
                  title: "Psychologie du Trading",
                  description: "Maîtrisez vos émotions pour prendre des décisions éclairées.",
                  icon: "🧠",
                  color: "linear-gradient(135deg, #2563eb, #1e40af)"
                }
              ].map((course: Course, index: number) => (
                <motion.div 
                  key={index} 
                  className="course-card group relative overflow-hidden rounded-2xl bg-gradient-to-br from-gray-800 to-gray-900 p-6 backdrop-blur-sm border border-gray-700/50 hover:border-blue-500/30 transition-all duration-300"
                  variants={item}
                  whileHover={{ 
                    y: -10,
                    boxShadow: '0 20px 40px -10px rgba(30, 58, 138, 0.3)'
                  }}
                >
                  <div className="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-300"
                    style={{
                      background: course.color || 'linear-gradient(135deg, #1e40af, #1e3a8a)'
                    }}
                  ></div>
                  
                  <div className="relative z-10">
                    <div className="w-16 h-16 rounded-xl bg-blue-900/30 flex items-center justify-center text-2xl mb-6 transform group-hover:scale-110 transition-transform duration-300">
                      {course.icon}
                    </div>
                    <h3 className="text-xl font-bold mb-3 text-white group-hover:text-white transition-colors duration-300">
                      {course.title}
                    </h3>
                    <p className="text-gray-300 group-hover:text-gray-100 transition-colors duration-300">
                      {course.description}
                    </p>
                    <button 
                      className="mt-6 text-sm font-medium text-blue-400 hover:text-white flex items-center group-hover:translate-x-1 transition-transform duration-300"
                    >
                      En savoir plus
                      <svg className="w-4 h-4 ml-2 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
                      </svg>
                    </button>
                  </div>
                </motion.div>
              ))}
            </motion.div>
          </div>
        </section>
      </div>

      {/* Pied de page */}
      <footer className="footer relative overflow-hidden py-12 bg-gradient-to-b from-gray-900 to-gray-950">
        <div className="container mx-auto px-4">
          <div className="grid grid-cols-1 md:grid-cols-4 gap-12 mb-12">
            <div>
              <div className="flex items-center mb-6">
                <img 
                  src="/images/Design_sans_titre_19.png" 
                  alt="TradeElite Pro Logo" 
                  className="h-10 mr-3"
                />
                <span className="text-2xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-white to-blue-200">TradeElite Pro</span>
              </div>
              <p className="text-gray-400">Formations d'excellence pour traders avertis et ambitieux.</p>
            </div>
            
            <div>
              <h4 className="text-white font-semibold mb-4 text-lg">Navigation</h4>
              <ul className="space-y-3">
                {['Accueil', 'Formations', 'À propos', 'Contact'].map((item, i) => (
                  <motion.li 
                    key={i}
                    whileHover={{ x: 5 }}
                    transition={{ type: 'spring', stiffness: 300 }}
                  >
                    <a href="#" className="text-gray-400 hover:text-white transition-colors">{item}</a>
                  </motion.li>
                ))}
              </ul>
            </div>
            
            <div>
              <h4 className="text-white font-semibold mb-4 text-lg">Ressources</h4>
              <ul className="space-y-3">
                {['Blog', 'Guides', 'Tutoriels', 'FAQ'].map((item, i) => (
                  <motion.li 
                    key={i}
                    whileHover={{ x: 5 }}
                    transition={{ type: 'spring', stiffness: 300 }}
                  >
                    <a href="#" className="text-gray-400 hover:text-white transition-colors">{item}</a>
                  </motion.li>
                ))}
              </ul>
            </div>
            
            <div>
              <h4 className="text-white font-semibold mb-4 text-lg">Contact</h4>
              <ul className="space-y-3">
                <li className="flex items-center text-gray-400">
                  <svg className="w-5 h-5 mr-2 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                  </svg>
                  contact@tradeelitepro.com
                </li>
                <li className="flex items-center text-gray-400">
                  <svg className="w-5 h-5 mr-2 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                  </svg>
                  +33 1 23 45 67 89
                </li>
                <li className="mt-6">
                  <div className="flex space-x-4">
                    {['twitter', 'facebook', 'instagram', 'linkedin'].map((social, i) => {
                      let iconPath = '';
                      switch(social) {
                        case 'twitter':
                          iconPath = 'M22.162 5.656a8.384 8.384 0 01-2.402.658A4.196 4.196 0 0021.6 4c-.82.488-1.719.83-2.656 1.015a4.182 4.182 0 00-7.126 3.814 11.874 11.874 0 01-8.62-4.37 4.168 4.168 0 00-.566 2.103c0 1.45.738 2.731 1.86 3.481a4.168 4.168 0 01-1.894-.523v.052a4.185 4.185 0 003.355 4.101 4.21 4.21 0 01-1.89.072A4.185 4.185 0 007.97 16.65a8.394 8.394 0 01-6.191 1.732 11.83 11.83 0 006.41 1.88c7.693 0 11.9-6.373 11.9-11.9 0-.18-.005-.362-.013-.54a8.496 8.496 0 002.087-2.165z';
                          break;
                        case 'facebook':
                          iconPath = 'M20 12c0-5.523-4.477-10-10-10S2 6.477 2 12c0 4.991 3.657 9.128 8.438 9.878v-6.987h-2.54V12h2.54V9.797c0-2.506 1.492-3.89 3.777-3.89 1.094 0 2.238.195 2.238.195v2.46h-1.26c-1.243 0-1.63.771-1.63 1.562V12h2.773l-.443 2.89h-2.33v6.988C18.343 21.128 22 16.991 22 12z';
                          break;
                        case 'instagram':
                          iconPath = 'M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.7-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0z';
                          break;
                        case 'linkedin':
                          iconPath = 'M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z';
                          break;
                      }
                      
                      return (
                        <motion.a 
                          key={i}
                          href="#" 
                          className="w-10 h-10 rounded-full bg-gray-800 flex items-center justify-center text-gray-300 hover:text-white hover:bg-blue-600 transition-colors"
                          whileHover={{ y: -3, scale: 1.1 }}
                          whileTap={{ scale: 0.95 }}
                        >
                          <span className="sr-only">{social}</span>
                          <svg className="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                            <path d={iconPath} />
                          </svg>
                        </motion.a>
                      );
                    })}
                  </div>
                </li>
              </ul>
            </div>
          </div>
          
          <div className="border-t border-gray-800 pt-8 flex flex-col md:flex-row justify-between items-center">
            <p className="text-gray-500 text-sm"> 2025 TradeElite Pro. Tous droits réservés.</p>
            <div className="flex space-x-6 mt-4 md:mt-0">
              {['Mentions légales', 'Politique de confidentialité', 'CGU', 'Cookies'].map((item, i) => (
                <a key={i} href="#" className="text-gray-500 hover:text-white text-sm transition-colors">
                  {item}
                </a>
              ))}
            </div>
          </div>
        </div>
        
        {/* Effet de particules */}
        {particles.map((particle, i) => (
          <motion.div
            key={i}
            className="absolute rounded-full bg-white/5"
            style={{
              width: particle.width,
              height: particle.height,
              left: particle.left,
              top: particle.top,
            }}
            animate={{
              y: [0, -Math.random() * 50 - 20],
              x: [0, (Math.random() - 0.5) * 40],
              opacity: [0.1, 0.5, 0.1]
            }}
            transition={{
              duration: Math.random() * 10 + 10,
              repeat: Infinity,
              repeatType: 'reverse'
            }}
          />
        ))}
      </footer>
    </div>
  );
};

export default Home;
